package Prompt.cmd;

public class Walk implements Cmd {
    @Override
    public void start(String args) {
        String[] argList = args.split(" ");
        String placeName = argList[0];
        
    }
}

/*Finalizando o código do mapa antes de fazer a função de ir para algum lugar */